spring:
  application:
    name: "accounts"
  profiles:
    active: "prod"
#  config:
#    import:
#    - "optional:configserver:http://localhost:8071/"
  rabbitmq:
    host: rabbit
    port: 5672
    username: "guest"
    password: "guest"
management:
  endpoints:
    web:
      exposure:
        include: "bus-refresh"