spring:
  application:
    name: "cards"
#  config:
#    import:
#    - "optional:configserver:http://localhost:8071/"
  profiles:
    active: "prod"
  rabbitmq:
    host: rabbit
    port: 5672
    username: "guest"
    password: "guest"
management:
  endpoints:
    web:
      exposure:
        include: "bus-refresh"